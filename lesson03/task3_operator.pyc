import socket

client = socket.socket()

server_addr = ('localhost', 10000)
client.connect(server_addr)

client.sendall('''{"a": 1, "b": 2, "o": "+"}''')

data = client.recv(16)

print data

client.close()
